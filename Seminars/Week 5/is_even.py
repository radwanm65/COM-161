def is_even(number):
    # determine whether number is even
    #f so return True, otherwise return False

    if (number % 2) == 0:
        status = True
    else
        status = False
    # end of if statement
    
    return status
#end of function is_even
